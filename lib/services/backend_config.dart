/// Centralized flag to enable/disable backend usage.
///
/// Dreamflow note: If you want to use Firebase or Supabase,
/// open the Firebase (or Supabase) panel in Dreamflow and complete setup.
/// After connecting, you can flip this flag to true and wire the backend.
class BackendConfig {
  // Set to true once backend is connected via Dreamflow panel.
  static const bool backendEnabled = false;
}
